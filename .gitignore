.Rproj.user
.Rhistory
.Rdata
.Rproj
.httr-oauth
.DS_Store
.quarto
inst/doc
/doc/
/Meta/
spat*

# Rendered pkgdown site — built and deployed by CI to the gh-pages branch;
# GitHub Pages serves from gh-pages (root), so the built site must not be tracked here.
docs/
