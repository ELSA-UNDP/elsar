% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{conditionally_subdivide_bbox}
\alias{conditionally_subdivide_bbox}
\title{Conditionally Subdivide Bounding Box into Grid Tiles Based on Geographic Extent}
\usage{
conditionally_subdivide_bbox(bbox_sf, degree_threshold = 10, tile_size_deg = 5)
}
\arguments{
\item{bbox_sf}{An \code{sf} object. Only the bounding box is used for subdivision logic.}

\item{degree_threshold}{Numeric. Threshold in degrees of lat/lon span at which to trigger subdivision. Default is 10.}

\item{tile_size_deg}{Numeric. Approximate tile width/height in degrees. Default is 5.}
}
\value{
An \code{sf} object. Either:
\itemize{
\item A grid of tiles (rectangular polygons) if subdivision is triggered, or
\item The original input \code{bbox_sf} unchanged.
}
}
\description{
This function checks whether the geographic extent of an \code{sf} object's bounding box
exceeds a specified threshold in degrees (longitude or latitude). If so, it splits
the bounding box into a regular grid of rectangular tiles of approximately \code{tile_size_deg}
degrees in size. If not, it simply returns the original input.
}
\details{
This is useful to improve performance when running spatial operations (e.g., \code{intersect})
on large countries or regions by breaking the work into spatial chunks that benefit
from spatial indexing.
}
\examples{
\dontrun{
  world <- sf::st_read(system.file("shape/nc.shp", package = "sf"))
  tiles <- conditionally_subdivide_bbox(world)
  plot(st_geometry(world))
  plot(st_geometry(tiles), add = TRUE, border = "red")
}

}
