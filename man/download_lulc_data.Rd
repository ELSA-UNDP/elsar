% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gee_helpers.R
\name{download_lulc_data}
\alias{download_lulc_data}
\title{Download LULC Data from Multiple Sources}
\usage{
download_lulc_data(
  boundary_layer,
  iso3,
  product = c("esri_10m", "dynamic_world", "esa_worldcover", "local"),
  gee_project = NULL,
  output_dir = here::here(),
  local_file = NULL,
  year = NULL,
  pus = NULL,
  aggregate_to_pus = FALSE,
  wait_time = 5,
  ...
)
}
\arguments{
\item{boundary_layer}{An \code{sf} object defining the spatial boundary of interest.}

\item{iso3}{Character. Three-letter ISO country code.}

\item{product}{Character. LULC product to download: "esri_10m" (default),
"dynamic_world", "esa_worldcover", or "local".}

\item{gee_project}{Character or NULL. Google Earth Engine cloud project ID.
Required for GEE-sourced products (esri_10m, dynamic_world, esa_worldcover).}

\item{output_dir}{Character. Directory for saving output. Defaults to \code{here::here()}.}

\item{local_file}{Character or NULL. Path to local GeoTIFF/COG file.
Required when \code{product = "local"}.}

\item{year}{Numeric or NULL. Specific year to download. If NULL, uses most recent.
For Dynamic World, this creates an annual mode composite. Ignored for ESA WorldCover
(fixed at 2021).}

\item{pus}{SpatRaster or NULL. Planning units raster for GEE-side aggregation.}

\item{aggregate_to_pus}{Logical. If TRUE and \code{pus} is provided, aggregate the
data to planning unit resolution in GEE before export. Default is FALSE.}

\item{wait_time}{Numeric. Maximum wait time for GEE exports in minutes. Default is 5.}

\item{...}{Additional arguments passed to underlying download functions.}
}
\value{
A \code{SpatRaster} object of the LULC data with attribute \code{lulc_product}
indicating the source, or NULL if download failed.
}
\description{
Unified interface for downloading Land Use/Land Cover (LULC) data from various
sources including ESRI Global LULC, Google Dynamic World, ESA WorldCover, or
loading local files.
}
\examples{
\dontrun{
# Download ESRI LULC (default)
lulc <- download_lulc_data(
  boundary_layer = ghana_boundary,
  iso3 = "GHA",
  gee_project = "my-project"
)

# Download Dynamic World annual composite
lulc_dw <- download_lulc_data(
  boundary_layer = ghana_boundary,
  iso3 = "GHA",
  product = "dynamic_world",
  gee_project = "my-project",
  year = 2023
)

# Download ESA WorldCover
lulc_esa <- download_lulc_data(
  boundary_layer = ghana_boundary,
  iso3 = "GHA",
  product = "esa_worldcover",
  gee_project = "my-project"
)

# Use local file
lulc_local <- download_lulc_data(
  boundary_layer = ghana_boundary,
  iso3 = "GHA",
  product = "local",
  local_file = "path/to/custom_lulc.tif"
)

# Download with GEE-side aggregation to planning units
lulc_agg <- download_lulc_data(
  boundary_layer = ghana_boundary,
  iso3 = "GHA",
  product = "esri_10m",
  gee_project = "my-project",
  pus = planning_units,
  aggregate_to_pus = TRUE
)
}
}
\seealso{
\code{\link[=get_lulc_classes]{get_lulc_classes()}}, \code{\link[=get_lulc_class_value]{get_lulc_class_value()}}, \code{\link[=list_lulc_products]{list_lulc_products()}},
\code{\link[=download_esri_lulc_data]{download_esri_lulc_data()}}, \code{\link[=download_dynamic_world_data]{download_dynamic_world_data()}},
\code{\link[=download_esa_worldcover_data]{download_esa_worldcover_data()}}, \code{\link[=load_local_lulc_data]{load_local_lulc_data()}}
}
