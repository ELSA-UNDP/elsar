% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elsar_load_data.R
\name{combine_layers}
\alias{combine_layers}
\title{Combine per-layer read results into a single sf, surfacing read failures}
\usage{
combine_layers(results, sources, noun = "layer")
}
\arguments{
\item{results}{List returned from \code{\link[=filter_sf]{filter_sf()}} calls; \code{NULL} = failed read.}

\item{sources}{Character vector of the file paths / layer names read, in the
same order as \code{results}, used in messages.}

\item{noun}{Character. \code{"file"} or \code{"layer"}, for messages.}
}
\value{
An \code{sf} object combining all successful reads (possibly zero rows).
}
\description{
Aggregates the results of several \code{\link[=filter_sf]{filter_sf()}} calls (one per file or
layer). \code{NULL} entries are failed reads; successful reads are \code{sf} objects
(possibly with zero rows). Unlike a bare \code{dplyr::bind_rows()}, this:
}
\details{
\itemize{
\item errors if \emph{every} read failed (rather than returning a \verb{0 x 0} tibble that
is not an \code{sf} object), and
\item warns, but continues, if \emph{some} reads failed, naming which.
}
}
\keyword{internal}
