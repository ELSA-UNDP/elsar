% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elsar_setup_gee.R
\name{elsar_setup_gee}
\alias{elsar_setup_gee}
\title{Set up the Earth Engine environment for elsar}
\usage{
elsar_setup_gee(
  python_version = "3.12",
  install_conda = interactive(),
  authenticate = interactive(),
  authenticate_drive = TRUE,
  gee_project = NULL
)
}
\arguments{
\item{python_version}{Character. Python version for a newly created
environment. Default \code{"3.12"}.}

\item{install_conda}{Logical. If no conda installation is found, may this
function install miniconda for you (via \code{reticulate::install_miniconda()})?
Defaults to \code{TRUE} in an interactive session and \code{FALSE} otherwise, so an
unattended run never silently downloads a conda installer.}

\item{authenticate}{Logical. Run the interactive Earth Engine and Google
Drive authentication flows after the environment is ready? Defaults to
\code{TRUE} in an interactive session, \code{FALSE} otherwise (the flows open a
browser and cannot complete unattended). See \code{authenticate_drive}.}

\item{authenticate_drive}{Logical. Whether the authentication step also
covers Google Drive (used to transfer GEE exports). Default \code{TRUE}. Ignored
when \code{authenticate} is \code{FALSE}.}

\item{gee_project}{Character or \code{NULL}. If supplied, the Earth Engine session
is initialised against this Cloud project ID as a final check that
everything works. Default \code{NULL} (authenticate only).}
}
\value{
Invisibly, a list with \code{env} (the environment name), \code{conda} (the
conda base directory), and \code{authenticated} (logical).
}
\description{
One-call bootstrap for the Google Earth Engine (GEE) workflow (the
\verb{download_*} functions). It:
}
\details{
\enumerate{
\item finds a conda installation, and - if none exists and you allow it -
installs a self-contained miniconda via \code{reticulate::install_miniconda()};
\item creates a \strong{persistent} conda environment (\code{\link[=elsar_gee_env]{elsar_gee_env()}}, default
\code{"elsar_ee"}) with \code{earthengine-api} installed, reusing it if it already
exists so the large download happens only once; and
\item optionally walks you through Earth Engine and Google Drive authentication.
}

Run this once, ahead of time, rather than letting the first \verb{download_*} call
build the environment mid-analysis. Afterwards, \code{\link[=elsar_check_setup]{elsar_check_setup()}} should
report the Earth Engine tier as ready.

You still need your \strong{own} Google Earth Engine account and an approved Cloud
project - this function configures your machine, not your Google access. See
the package's \code{SUPPORT.md} for what that entails.
}
\examples{
\dontrun{
# Full guided setup, including authentication (interactive session):
elsar_setup_gee()

# Just build the environment now; authenticate later on first use:
elsar_setup_gee(authenticate = FALSE)

# Build and verify against a specific Cloud project:
elsar_setup_gee(gee_project = "my-ee-project")
}

}
\seealso{
\code{\link[=elsar_check_setup]{elsar_check_setup()}} to verify the result.
}
