% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_custom_mollweide_projection.R
\name{make_custom_mollweide_projection}
\alias{make_custom_mollweide_projection}
\alias{make_custom_projection}
\title{Create a custom Mollweide projection centred on the planning region}
\usage{
make_custom_mollweide_projection(
  boundary,
  output_path = NULL,
  iso3_column = "iso3cd",
  iso3 = NULL
)

make_custom_projection(
  boundary,
  output_path = NULL,
  iso3_column = "iso3cd",
  iso3 = NULL
)
}
\arguments{
\item{boundary}{\code{sf} object of the boundary of the planning region. Should match iso3 country code.}

\item{output_path}{An optional output path for the created file.}

\item{iso3_column}{A string of the name of where iso3 information can be found in a dataset.}

\item{iso3}{The iso3 country code (character) of the country of interest. Used only to name the projection / output file; the projection maths uses the boundary geometry.}
}
\value{
A \code{wkt} CRS string for a Mollweide projection centred on the planning region
}
\description{
Builds an equal-area \strong{Mollweide} projection whose central meridian is
the longitude of the planning region's centre, and returns it as a WKT CRS
string. The boundary is transformed to WGS84 internally to compute the centre,
so any input CRS is handled correctly.

\code{make_custom_projection()} is a deprecated alias for
\code{make_custom_mollweide_projection()} - the projection has always been
Mollweide; the clearer name should be preferred.
}
\examples{
boundary <- make_boundary(
  boundary_in = boundary_dat,
  iso3 = "NPL",
  iso3_column = "iso3cd"
)

wkt <- make_custom_mollweide_projection(boundary = boundary, iso3 = "NPL")
}
