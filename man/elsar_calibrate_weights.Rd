% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_weights.R
\name{elsar_calibrate_weights}
\alias{elsar_calibrate_weights}
\title{Calibrate feature weights for an ELSA prioritization}
\usage{
elsar_calibrate_weights(
  features,
  impacts,
  zones_pu,
  planning_units,
  budgets,
  lockins = NULL,
  feature_totals = NULL,
  wgt_scale = 2,
  gap = 0.05,
  solver = c("gurobi", "highs", "default"),
  threads = 4,
  time_limit = 3600,
  solve_retries = 2,
  freeze_tol = 0.01,
  freeze_patience = 3,
  max_iter = 200,
  verbose = TRUE
)
}
\arguments{
\item{features}{A \code{SpatRaster} of feature layers, ideally normalised to \verb{[0, 1]}.
Layer names are used as the feature identifiers throughout.}

\item{impacts}{A \code{data.frame} mapping features to action zones. Must contain a
\code{feature} column (matching \code{names(features)}) and one numeric column per action
zone giving that feature's zone-contribution ("impact"), typically in \verb{[0, 1.5]}.
Zone column names must match \code{names(zones_pu)}.}

\item{zones_pu}{A named \code{list} of single-layer \code{SpatRaster} planning-unit masks
(\code{1} = available, \code{NA} = unavailable), one per action zone. If lock-ins are to be
enforced, pass masks that already exclude lock-in cells from other zones. Names
define the action zones and their order.}

\item{planning_units}{A single-layer \code{SpatRaster} of all planning units in the
study region (\code{1}/\code{NA}). Used as the objective budget and the denominator of
area targets.}

\item{budgets}{A named numeric vector of per-zone area caps, each a percentage of
the study-area planning-unit count. Names must match \code{names(zones_pu)}.}

\item{lockins}{Optional named \code{list} of \code{SpatRaster} lock-in areas per action zone.
Used only to raise a zone's target to its locked-in coverage when the lock-in
exceeds the nominal budget (feasibility floor).}

\item{feature_totals}{Optional named numeric vector of raw (non-impact-weighted)
feature totals used as the representation denominator. Computed from \code{features}
via \code{terra::global(..., "sum")} when \code{NULL} (the default).}

\item{wgt_scale}{Maximum weight increment applied per iteration (default \code{2}).}

\item{gap}{Solver optimality-gap tolerance (default \code{0.05}). Note: the ELSA
max-utility problem is typically solved to an exact optimum at the root node, so
this tolerance rarely binds.}

\item{solver}{Solver backend: \code{"gurobi"} (default), \code{"highs"}, or \code{"default"}
(prioritizr's best available). \code{"gurobi"} requires the \code{gurobi} package.}

\item{threads}{Number of solver threads (default \code{4}).}

\item{time_limit}{Per-solve time limit in seconds (default \code{3600}).}

\item{solve_retries}{Number of times to retry a failed solve (default \code{2}), to
survive transient solver or cloud-connection errors (e.g. a dropped connection
to a Gurobi Compute Server). A short pause separates attempts.}

\item{freeze_tol}{Convergence threshold on the maximum per-feature change in
representation between iterations (default \code{0.01}). When the allocation stops
changing by more than this, further additive reweighting cannot move the (exactly
solved) optimum.}

\item{freeze_patience}{Number of consecutive frozen iterations required to stop
(default \code{3}).}

\item{max_iter}{Safety cap on the number of calibration iterations (default \code{200}).}

\item{verbose}{Logical; emit progress messages via \code{\link[=log_message]{log_message()}} (default \code{TRUE}).}
}
\value{
A named \code{list} of class \code{elsar_calibration} with elements:
\describe{
\item{weights}{A \code{tibble} with columns \code{feature} and \code{weight} (the calibrated
per-feature weight).}
\item{weight_matrix}{The feature x zone weight matrix actually used (the
\code{do_nothing} column is \code{0}).}
\item{representation}{A \code{tibble} with per-feature \code{a} (achieved representation),
\code{a_max}, \code{ratio = a / a_max}, and \code{delta_pct} shortfall.}
\item{a_max}{Named numeric vector of single-feature maximum representations.}
\item{trajectory}{A \code{tibble} of per-iteration diagnostics (spread, max change,
weight range, shortfall summary).}
\item{converged}{Logical; \code{TRUE} if the loop stopped on the freeze rule rather
than \code{max_iter}.}
\item{iterations}{Number of calibration iterations run.}
\item{meta}{A list of run metadata (solver, gap, wgt_scale, package versions).}
}
}
\description{
Iteratively computes a set of per-feature weights so that all features achieve
more even representation relative to what each could attain on its own (its
single-feature maximum). Under-represented features receive larger weights;
well-represented features receive none. This is the pre-calibration step of the
ELSA workflow: the resulting weights are multiplied by any user-supplied weights
when the deployed tool solves the prioritization.
}
\details{
The routine reproduces the ELSA calibration framework exactly (it is not a new
objective): (1) each feature's maximum attainable representation \code{a_max} is
obtained by solving the zoned maximum-utility problem with that feature alone
weighted; (2) the problem is solved with equal weights to obtain each feature's
baseline representation and its relative shortfall
\code{delta = (a - a_max) / a_max * 100}; (3) weights are adjusted iteratively with the
additive, increase-only rule
\code{adj = wgt_scale - (delta - min(delta)) / (max(delta) - min(delta)) * wgt_scale},
re-solving and recomputing shortfalls, until the allocation stops changing. The
weight set giving the most even representation (smallest spread of \code{delta}) is
returned.

Requires the \code{prioritizr} package (and a supported solver, e.g. \code{gurobi} or
\code{highs}); both are optional dependencies.
}
\examples{
\dontrun{
cal <- elsar_calibrate_weights(
  features       = feature_stack,
  impacts        = impact_df,       # cols: feature, protect, restore, manage
  zones_pu       = list(protect = pu_protect, restore = pu_restore, manage = pu_manage),
  planning_units = pu_all,
  budgets        = c(protect = 30, restore = 18.564, manage = 5),
  solver         = "gurobi"
)
cal$weights
}

}
