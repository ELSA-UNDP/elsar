% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_flood_abatement_opportunities.R
\name{make_flood_abatement_opportunities}
\alias{make_flood_abatement_opportunities}
\title{Create a Flood Abatement Opportunities Raster}
\usage{
make_flood_abatement_opportunities(
  gfd_raster,
  ndvi_raster,
  pus,
  iso3,
  output_path = NULL
)
}
\arguments{
\item{gfd_raster}{A \code{SpatRaster} representing flood risk data from the Global Flood Database.}

\item{ndvi_raster}{A \code{SpatRaster} representing NDVI data.}

\item{pus}{A \code{SpatRaster} defining the planning unit (PU) grid.}

\item{iso3}{A character string representing the ISO3 country code.}

\item{output_path}{A character string specifying the output directory for saving the raster (optional).}

\item{threads}{A logical value indicating whether to use multi-threaded processing where supported (default: \code{TRUE}).}
}
\value{
A \code{SpatRaster} representing flood abatement opportunities, rescaled between 0 and 1.
If \code{output_path} is provided, the raster is saved as a Cloud-Optimized GeoTIFF (COG).
}
\description{
This function generates a raster that identifies flood abatement opportunities
by integrating the Global Flood Database (GFD) and NDVI (Normalized Difference Vegetation Index).
The function normalizes NDVI and flood risk data, then calculates potential areas for flood
abatement based on their relationship.
}
\examples{
\dontrun{
flood_abatement <- make_flood_abatement_opportunities(
  gfd_raster = terra::rast("gfd_flood_database.tif"),
  ndvi_raster = terra::rast("mod13q1_2022_ndvi_wgs84.tif"),
  pus = planning_units,
  iso3 = "NPL",
  output_path = "path/to/output"
)
}
}
