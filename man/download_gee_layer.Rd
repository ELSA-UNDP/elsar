% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gee_helpers.R
\name{download_gee_layer}
\alias{download_gee_layer}
\title{Download and Process a GEE Raster Layer into a Cloud-Optimized GeoTIFF}
\usage{
download_gee_layer(
  boundary_layer,
  iso3,
  gee_project,
  asset_id,
  file_prefix,
  output_dir = here::here(),
  scale = 10,
  datatype = "INT1U",
  googledrive_folder = NULL,
  wait_time = 5
)
}
\arguments{
\item{boundary_layer}{An \code{sf} object defining the spatial boundary of interest.}

\item{iso3}{Character. Three-letter ISO country code for filename generation.}

\item{gee_project}{Character. Google Earth Engine cloud project ID. This is
required and must be a valid GEE project you have access to. Find your
project ID in the \href{https://code.earthengine.google.com/}{GEE Code Editor}.}

\item{asset_id}{Character. Earth Engine ImageCollection asset ID
(e.g., "projects/sat-io/open-datasets/...").}

\item{file_prefix}{Character. Prefix for export filename and GEE task description.}

\item{output_dir}{Character. Path to directory for saving the final raster file.
Defaults to project root via \code{here::here()}.}

\item{scale}{Numeric. Resolution of the exported image in meters. Default is 10.}

\item{datatype}{Character. Output datatype (GDAL style), e.g., "INT1U" or "FLT4S".
Default is "INT1U".}

\item{googledrive_folder}{Character or NULL. Google Drive folder name for exports.
Currently defaults to NULL (Drive root) to avoid a GEE folder duplication bug.}

\item{wait_time}{Numeric. Maximum time in minutes to wait for the GEE export
to appear in Google Drive. Default is 5. Increase for large exports.}
}
\value{
A \code{SpatRaster} object written to disk, or NULL if export timed out.
}
\description{
Generic function for downloading any raster data from Google Earth Engine.
Handles authentication, export management, file downloading, and processing
into a local Cloud-Optimized GeoTIFF. Exports to Google Drive root to avoid
GEE folder duplication bugs.
}
\details{
For most use cases, prefer the higher-level wrapper functions like
\code{\link[=download_esri_lulc_data]{download_esri_lulc_data()}} or \code{\link[=download_global_pasture_data]{download_global_pasture_data()}} which
have sensible defaults for common datasets.
}
\examples{
\dontrun{
# Download ESRI LULC data for Ghana
lulc <- download_gee_layer(
  boundary_layer = ghana_boundary,
  iso3 = "GHA",
  gee_project = "my-gee-project",
  asset_id = "projects/sat-io/open-datasets/landcover/ESRI_Global-LULC_10m_TS",
  file_prefix = "esri_10m_lulc",
  scale = 10,
  datatype = "INT1U"
)
}
}
\seealso{
\code{\link[=download_esri_lulc_data]{download_esri_lulc_data()}}, \code{\link[=download_global_pasture_data]{download_global_pasture_data()}}
}
