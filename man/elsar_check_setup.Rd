% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elsar_check_setup.R
\name{elsar_check_setup}
\alias{elsar_check_setup}
\title{Diagnose your elsar setup}
\usage{
elsar_check_setup(
  tiers = c("core", "protected_areas", "earth_engine"),
  quiet = FALSE
)
}
\arguments{
\item{tiers}{Character vector naming which tiers to check. Any of
\code{"core"}, \code{"protected_areas"}, \code{"earth_engine"}. Defaults to all three.}

\item{quiet}{Logical. If \code{TRUE}, suppress the printed report and only return
the results invisibly. Default \code{FALSE}.}
}
\value{
Invisibly, a \code{data.frame} with one row per check and the columns
\code{tier}, \code{item}, \code{status} (\code{"ok"}, \code{"missing"}, \code{"warn"}, or \code{"info"}),
\code{detail}, and \code{fix}. Returned invisibly so it can be inspected
programmatically while the human-readable report prints to the console.
}
\description{
Reports, tier by tier, what elsar needs and whether it is present on your
machine, with the exact command to fix anything that is missing. It is safe
to run any time: it makes no changes, needs no credentials, and never errors
out - a failed probe is reported as a finding, not thrown.
}
\details{
elsar has three tiers with different setup requirements (see the package's
\code{SUPPORT.md}):
\itemize{
\item \strong{Core} - boundaries, planning units, normalisation, zone builders from
local rasters, and plotting. Needs only the geospatial R stack (GDAL, GEOS,
PROJ, installed with \code{sf}/\code{terra}). Works offline.
\item \strong{Protected areas} - \code{make_protected_areas(from_wdpca = TRUE)}, which needs
\code{wdpar}, a Chromium browser, and the \code{libarchive} system library.
\item \strong{Earth Engine} - the \verb{download_*} functions, which need \code{reticulate},
\code{googledrive}, a conda/Python environment with \code{earthengine-api}, and a
Google Earth Engine account with an approved Cloud project.
}

If you are filing a bug report about the \verb{download_*} functions, paste the
output of \code{elsar_check_setup()} into the report.
}
\examples{
# Safe to run anywhere - no changes, no credentials required.
elsar_check_setup()

# Just the core geospatial stack:
elsar_check_setup(tiers = "core")

# Capture the results without printing:
res <- elsar_check_setup(quiet = TRUE)
res[res$status != "ok", ]

}
