% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{save_raster}
\alias{save_raster}
\title{Save a Raster as a Cloud Optimized GeoTIFF (COG)}
\usage{
save_raster(raster, filename, datatype = "FLT4S", resampling = NULL)
}
\arguments{
\item{raster}{A \code{SpatRaster} object to be saved.}

\item{filename}{Character. Full file path (including \code{.tif} extension) where the raster will be saved.}

\item{datatype}{Character. GDAL data type to use for saving the raster (e.g. \code{"FLT4S"} for float, \code{"INT1U"} for unsigned byte). Default is \code{"FLT4S"}.}

\item{resampling}{Character or NULL. Overview resampling method (e.g. \code{"average"}, \code{"nearest"}, \code{"mode"}). If \code{NULL} (default), it is chosen from \code{datatype}: \code{"average"} for float, \code{"nearest"} for integer. Use \code{"mode"} for multi-class categorical layers such as land cover so overviews keep the dominant class.}
}
\value{
None. The function is called for its side effect of saving the raster to disk.
}
\description{
This helper function saves a \code{SpatRaster} to disk as a Cloud Optimized GeoTIFF (COG) with ZSTD compression.
It automatically sets the appropriate GDAL predictor based on the data type to optimize file size and performance.
For floating point rasters (\code{FLT4S}), Predictor 3 is used, and for integer rasters, Predictor 2 is used, which improves compressibility.
A nodata value is also automatically defined and applied: 255 for integer rasters (e.g. \code{"INT1U"}) and \code{NaN} for floating point rasters.
}
\details{
This function should be used across processing functions to ensure consistent raster output formats.
}
\examples{
\dontrun{
save_raster(my_raster, "output/my_raster.tif", datatype = "INT1U")
save_raster(my_raster, "output/my_raster_float.tif", datatype = "FLT4S")
}

}
